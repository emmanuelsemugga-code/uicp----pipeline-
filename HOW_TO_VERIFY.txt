HOW TO VERIFY PHASES 1‑5 (External Adversarial Package)
========================================================

1. PLACE ALL REQUIRED FILES IN ONE DIRECTORY

   Public wrappers and verification scripts you received:
     · Phase 1: normalize.py, verify_phase1_claims.py
     · Phase 2: phase2_public.py, verify_phase2_claims.py
     · Phase 3: phase3_public.py, verify_phase3_claims.py
     · Phase 4: phase4_public.py, verify_phase4_claims.py
     · Phase 5: phase5_public.py, verify_phase5_claims.py

   Convenience script:
     · verify_all_phases.py

   Internal engines (provided under controlled disclosure):
     · normalize_v05.py, phase2_engine.py, phase3_engine.py,
       phase4_engine.py, phase5_engine.py

2. VERIFY ALL FIVE PHASES WITH ONE COMMAND

     python3 verify_all_phases.py

   Expected output:

     ALL PHASES VERIFIED — Phases 1‑5 are externally proven.

3. VERIFY A SINGLE PHASE

     python3 verify_phase1_claims.py
     python3 verify_phase2_claims.py
     python3 verify_phase3_claims.py
     python3 verify_phase4_claims.py
     python3 verify_phase5_claims.py

   Each script prints individual claim results and a final VERDICT.

4. WHAT YOU ARE PROVING

   · Phase 1 — syntactic normalization: determinism, boundedness,
     termination, fixed‑point, commutativity, absorption,
     identity bijection, serialization invariance, structural
     stability, escape‑channel containment.
   · Phase 2 — single‑variable semantic analysis: equivalence,
     dominance, conflict, execution, OUT_OF_SCOPE preservation,
     multi‑variable extended fields, identity‑ledger completeness.
   · Phase 3 — multi‑variable canonicalization: redundancy
     elimination, conflict detection, nonlinear preservation,
     determinism, identity‑ledger completeness.
   · Phase 4 — runtime enforcement: contract integrity, binding
     validation, enforceable constraint checking, non‑enforceable
     constraint pass‑through, identity immutability, determinism,
     cryptographic chain integrity, sanitised model‑facing response,
     output contract compliance.
   · Phase 5 — trust and audit: objective commitment with Ed25519
     signatures, verifiable compliance proofs, human‑gated override
     controls, append‑only audit log with cryptographic chaining,
     private‑key safety enforcement, third‑party proof verification
     without engine access.

5. DISCLOSURE BOUNDARY

   These scripts call only the public wrappers.  No internal
   algorithms, pipeline logic, or design maps are exposed.
   The internal engines are required to execute the wrappers,
   but are not part of this public package.
